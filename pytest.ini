[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra --strict-markers
markers =
    slow: marks tests that load NLP models (deselect with '-m "not slow"')
filterwarnings =
    ignore::DeprecationWarning
